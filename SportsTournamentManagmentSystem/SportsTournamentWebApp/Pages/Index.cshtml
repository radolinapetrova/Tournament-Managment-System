@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<link rel="stylesheet" href="./css/index.css">

@for (int i = 0; i < @Model.Tournaments.Count; i++)
{
    <div class="tr">
        <a href="/TournamentInfo?id=@Model.Tournaments[i].Id">@Model.Tournaments[i].ToString()</a>
    </div>
    
}
